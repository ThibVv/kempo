{
  "name": "kempo-workspace",
  "version": "1.2.0",
  "private": true,
  "workspaces": [
    "backend",
    "front"
  ],
  "scripts": {
    "install:backend": "cd backend && pnpm install",
    "install:frontend": "cd front && npm ci",
    "test:backend": "cd backend && pnpm run test:coverage",
    "test:frontend": "cd front && npm run test:coverage"
  }
}
